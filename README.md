# links.rythmhacks.ca

## Setup Instructions
1. Clone the repository<br/>
```
git clone https://github.com/RythmHacks/links.rythmhacks.ca.git
```
2. Type `npm i` to install dependencies
3. Write some code and run `npm run dev` to start the web app
